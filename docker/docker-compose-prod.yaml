version: "3.9"

services:
  fernwartung:
    image: nginx:stable-alpine
    container_name: rustdesk-download-portal
    ports:
      - "9100:80"
    volumes:
      - /data/fernwartung/html:/usr/share/nginx/html:ro
    restart: unless-stopped
